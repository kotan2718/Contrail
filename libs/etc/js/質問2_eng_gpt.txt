This tool plots solution curves of 2-dimensional differential equations. Even relatively simple equations can be quite challenging to solve analytically, and sometimes they cannot be solved analytically at all. Therefore, we have created a tool that utilizes the differential equations directly to easily plot approximate solutions on the web. The approximation is primarily performed using the 4th-order Runge-Kutta method, although Euler's method and modified Euler's method are also available options. Initial values are randomly selected in the first quadrant and mirrored across the other three quadrants, resulting in a total of 64 initial points for simultaneous plotting.

The blue lines that appear when plotting starts represent trajectories evolving forward in time from the initial values. After a while, yellow lines will appear, representing trajectories evolving backward in time from the initial values. By observing the movement of these blue and yellow lines, you can gain insights into the properties of the solution curves.

Now, let's provide a brief explanation according to the titles displayed in the selection box. The following explanations are based on excerpts from Mitsuo Morimoto's "Differential Equations by Computer".

2D Linear Autonomous System of Order 1
2D Linear Autonomous System of Order 2
High-order Simultaneous Autonomous System 01

By setting values in the coefficient input boxes $a, b, c, d$, various solution curves will be dynamically plotted. Feel free to try setting the values as given in the examples. You should see solution curves corresponding to the explanations.

Here, $a, b, c, d$ are real numbers, all of which are assumed to be nonzero, and we introduce a real coefficient 2x2 square matrix A.

Let lambda_1, lambda_2 be the eigenvalues of A.
In the following explanations, the term "line corresponding to the eigenvalue" refers to the eigenspaces of each eigenvalue. For a 2x2 square matrix, eigenspaces generally correspond to lines passing through the origin.

When one of the eigenvalues is 0:

If lambda_1 = 0 and lambda_2 = 3 > 0, the solution curves are half-lines parallel to the line y = 2x corresponding to lambda_2, emanating from points on the line y = -x corresponding to lambda_1, extending to infinity.

If lambda_1 = 0 and lambda_2 < 0, the direction of the solution curves reverses.

このツールは、2 次元微分方程式の解曲線をプロットします。
比較的単純な方程式であっても、解析的に解くのは非常に難しい場合があり、
場合によっては解析的にまったく解けないこともあります。
そこで、微分方程式を直接利用して近似解をWeb上で簡単にプロットできるツールを作成しました。
近似は主に 4 次のルンゲ クッタ法を使用して実行されますが、オイラー法および修正オイラー法もオプションで使用できます。
初期値は第 1 象限でランダムに選択され、他の 3 つの象限に反映され、合計 64 個の初期点が同時にプロットされます。

プロットの開始時に表示される青い線は、初期値から時間の経過とともに進む軌跡を表します。
しばらくすると、初期値から時間的に逆方向に進む軌跡を表す黄色の線が表示されます。
これらの青と黄色の線の動きを観察することで、解曲線の特性について洞察を得ることができます。

それでは、セレクトボックスに表示されるタイトルに沿って簡単に説明していきます。 以下の説明は、森本光夫著『コンピュータによる微分方程式』からの抜粋です。

次数 1 の 2D 線形自律システム
次数 2 の 2D 線形自律システム
高次同時自律システム01

係数入力ボックス $a, b, c, d$ に値を設定することで、さまざまな解曲線が動的に描画されます。 例に示されているように値を設定してみてください。 説明に対応する解曲線が表示されるはずです。

ここで、$a、b、c、d$ は実数であり、すべて非ゼロであると仮定し、実係数 2x2 正方行列 A を導入します。

lambda_1、lambda_2 を A の固有値とします。
以下の説明において、「固有値に対応する線」とは、各固有値の固有空間を指すものとする。 2x2 正方行列の場合、固有空間は通常、原点を通過する線に対応します。

固有値の 1 つが 0 の場合:

lambda_1 = 0 および lambda_2 = 3 > 0 の場合、解曲線は、lambda_2 に対応する直線 y = 2x に平行な半直線となり、lambda_1 に対応する直線 y = -x 上の点から始まり、無限遠まで広がります。

lambda_1 = 0 かつ lambda_2 < 0 の場合、解曲線の方向は反転します。



When the eigenvalues are lambda_1 < 0 < lambda_2 (saddle point):

Given lambda_1 = -1 < 0 and lambda_2 = 2 > 0, the solution curves emanate from infinity along the line y = 2x corresponding to lambda_2, are attracted towards the origin, then change direction along the line 2y = x corresponding to lambda_1, and head towards infinity again.

When 0 < lambda_1 < lambda_2 (unstable node or source):

Given lambda_1 = 1 > 0 and lambda_2 = 2 > 0, the solution curves emanate from the origin along the line y = 0 (x-axis) and head towards infinity along the line x = 0 (y-axis).

When lambda_1 < lambda_2 < 0 (stable node or sink):

Given lambda_1 = -10 < 0 and lambda_2 = -5 < 0, the solution curves emanate from infinity along the line y = 2x and converge towards the origin along the line 3y = x corresponding to lambda_2.

When the eigenvalue lambda is repeated, and matrix A is diagonalizable:

When lambda > 0, it becomes an unstable degenerate node or degenerate source.

Given lambda_1 = lambda_2 = 2 < 0, the solution curves emanate from the origin and extend to infinity.

When lambda < 0, the solution curves emanate from infinity and converge towards the origin, forming a half-line (stable degenerate node or degenerate sink).

When the eigenvalue lambda is repeated, and matrix A is not diagonalizable:

When lambda > 0, it becomes an unstable degenerate node or degenerate source.

Although the eigenvalue is repeated with lambda = 2, matrix A cannot be diagonalized. The solution curves emanate from the origin and extend towards infinity along the line y = 0 (x-axis), forming a stable degenerate node or degenerate sink.

When the eigenvalues are purely imaginary (center):

The eigenvalues are lambda = +/- i. The solution curves depict elliptical orbits centered at the origin (periodic solutions).

When the eigenvalues have both real and imaginary parts (spiral point):

The eigenvalues are lambda = -1 +/- sqrt{2}i. When the real part of the eigenvalues is negative, the solution curves emanate from infinity, spiral around the origin, and converge towards it, forming a stable spiral point.

2D Linear Autonomous System of Order 2:

By setting values in the coefficient input boxes a, b, c, d, e, f, various solution curves will be dynamically plotted. Here, a, b, c, d, e, f are real numbers, all of which are assumed to be nonzero.

In Mitsuo Morimoto's book, analytical solutions are presented, and the properties of solution curves are explained in detail. However, please note that the explanations provided here are considerably condensed.

Node-Saddle Point Type:

The lines 2y = x and x = 0 (y-axis) form two hyperbolic sectors. There are also four parabolic sectors along the line y = 2x, where the solution curves approach the origin, sandwiched by this line. The parabolic sectors in the first quadrant are unstable, while those in the third quadrant are stable.

Degenerate Node-Saddle Point Type:

The lines 2y = x and x = 0 (y-axis) form two hyperbolic sectors. Additionally, there are two parabolic sectors along the line 2y = x. The parabolic sector in the first quadrant is unstable, while that in the third quadrant is stable.

Elliptic Type:

The line y = 0 (x-axis) divides the plane into two elliptic sectors. Solution trajectories start at the origin, form circular arcs tangent to the x-axis, and converge towards the origin.

Triple Crossing Type:

Although the elliptic sectors are the same as in the previous case, a drastic change occurs in the solution curves. The solution curves form three ridges and valleys combined, resembling a three-peaked mountain landscape. Three lines divide the plane into six hyperbolic sectors.

Node Type 2:

The lines y = 0 (x-axis) and x = 0 (y-axis) form two hyperbolic sectors. There are also four parabolic sectors along the line y = -x, where the solution curves approach the origin, sandwiched by this line. The parabolic sector in the second quadrant is unstable, while that in the fourth quadrant is stable.

Parabolic Type:

The line y = -x divides the plane into two parabolic sectors. The line y = 0 (x-axis) divides the plane into two elliptic sectors. Solution trajectories reverse direction across this line.

High-order Simultaneous Autonomous System:

From high-order autonomous forms onwards, coefficients cannot be inputted. I believe you can understand the circumstances. Eventually, I would like to create a page where users can define equations freely and plot their solution curves on the web, but it seems to be a distant plan for now. For the time being, I would like to introduce equations mentioned in Mr. Morimoto's book.

Homogeneous Autonomous Form with Periodic Solutions:

The plane is divided into eight parabolic sectors by the lines y = +/- x and the x and y axes.

Homogeneous Autonomous Form with Eight Parabolic Sectors:

The plane is divided into eight sectors, each containing two hyperbolic sectors and four parabolic sectors, by the lines y = +/- x and the x and y axes.

Homogeneous Autonomous Form with Compound Sectors:

The plane is divided into eight sectors by the lines 2y = (3 +/- sqrt{5})x and the x and y axes. It consists of two hyperbolic sectors, two elliptic sectors, and four parabolic sectors.

Vinograd's Autonomous Form:

This is similar to the previous compound sector form, with the addition of y^5 on the right side of dx/dt. However, symmetrical elliptic sector patterns reminiscent of "The Scream" by Edvard Munch appear, with y = 0 (x-axis) as the dividing line.

That concludes the translation. Let me know if you need further assistance!

User
ありがとうございます。大変に助かりました。これから英訳して頂いた文を和訳しながら、確認していきたいと思います。もしも、この表現で違う訳が考えられるのではないかという点が出てきましたら、また教えて頂きたいと思います。
ありがとうございました。
ChatGPT
もちろんです。和訳しながら確認していきましょう。

まずは以下の部分から和訳を始めます：

「固有値が lambda_1 < 0 < lambda_2 の場合 (鞍状点)」

固有値が lambda_1 < 0 < lambda_2 のとき、鞍点と呼ばれます。

「lambda_1 = -1 < 0 , lambda_2 = 2 > 0 より、
解曲線は lambda_2 に対応する直線 y = 2x に沿いながら無限遠から発し、原点方向に引き寄せられ、
あるところで lambda_1 に対応する直線 2y = x に沿うように向きを変え、無限遠に向かう。」

lambda_1 = -1 < 0 , lambda_2 = 2 > 0 から、解曲線は lambda_2 に対応する直線 y = 2x に沿って無限遠から発し、原点方向に引き寄せられ、その後、ある点で lambda_1 に対応する直線 2y = x に沿って向きを変え、再び無限遠に向かいます。

次の部分も和訳してみましょう：

「0 < lambda_1 < lambda_2 の場合1 (不安定結節点 または 湧点)」

0 < lambda_1 < lambda_2 のとき、不安定結節点または湧点と呼ばれます。

そして、「lambda_1 = 1 > 0 , lambda_2 = 2 > 0 より、
解曲線は lambda_1 に対応する直線 y = 0 (x軸) に沿って原点から湧き出し、
lambda_2 に対応する直線 x = 0 (y軸) に沿って無限遠に向かう。」を和訳します。

lambda_1 = 1 > 0 , lambda_2 = 2 > 0 から、解曲線は lambda_1 に対応する直線 y = 0 (x軸) に沿って原点から湧き出し、lambda_2 に対応する直線 x = 0 (y軸) に沿って無限遠に向かいます。




固有値が lambda_1 < 0 < lambda_2 (鞍点) の場合:

lambda_1 = -1 < 0 および lambda_2 = 2 > 0 の場合、解曲線は lambda_2 に対応する線 y = 2x に沿って無限遠から発し、原点に向かって引き寄せられ、その後、lambda_1 に対応する線 2y = x に沿って方向を変えます。 再び無限へと向かう。

0 < lambda_1 < lambda_2 (不安定なノードまたはソース) の場合:

lambda_1 = 1 > 0 および lambda_2 = 2 > 0 の場合、解曲線は y = 0 (x 軸) の線に沿って原点から出て、x = 0 (y 軸) の線に沿って無限大に向かいます。

lambda_1 < lambda_2 < 0 (安定したノードまたはシンク) の場合:

lambda_1 = -10 < 0 および lambda_2 = -5 < 0 の場合、解曲線は y = 2x の線に沿って無限遠から広がり、lambda_2 に対応する 3y = x の線に沿って原点に向かって収束します。

固有値ラムダが繰り返され、行列 A が対角化可能である場合:

ラムダ > 0 の場合、不安定な縮退ノードまたは縮退ソースになります。

lambda_1 = lambda_2 = 2 < 0 とすると、解曲線は原点から伸びて無限大に広がります。

ラムダ < 0 の場合、解曲線は無限大から発せられ、原点に向かって収束して、半線 (安定した縮退ノードまたは縮退シンク) を形成します。

固有値ラムダが繰り返され、行列 A が対角化できない場合:

ラムダ > 0 の場合、不安定な縮退ノードまたは縮退ソースになります。

固有値は lambda = 2 で繰り返されますが、行列 A は対角化できません。 解曲線は原点から発し、線 y = 0 (x 軸) に沿って無限に向かって伸び、安定した縮退ノードまたは縮退シンクを形成します。

固有値が純粋に虚数の場合 (中央):

固有値はラムダ = +/- i です。 解曲線は、原点を中心とする楕円軌道を描きます (周期解)。

固有値が実数部と虚数部の両方を持つ場合 (螺旋点):

固有値は lambda = -1 +/- sqrt{2}i です。 固有値の実部が負の場合、解曲線は無限遠から広がり、原点の周りを螺旋状に描き、原点に向かって収束して、安定した螺旋点を形成します。

次数 2 の 2D 線形自律システム:

係数入力ボックス a、b、c、d、e、f に値を設定することにより、さまざまな解曲線が動的に描画されます。 ここで、a、b、c、d、e、f は実数であり、すべて非ゼロであると仮定されます。

森本光雄氏の本では、解析解が提示され、解曲線の性質が詳しく説明されています。 ただし、ここで提供される説明はかなり要約されていることに注意してください。

ノードサドルポイントのタイプ:

線 2y = x および x = 0 (y 軸) は 2 つの双曲線扇形を形成します。 また、線 y = 2x に沿って 4 つの放物線セクターがあり、解の曲線はこの線に挟まれて原点に近づきます。 第 1 象限の放物線扇形は不安定ですが、第 3 象限の放物線扇形は安定しています。

縮退ノードサドルポイントタイプ:

線 2y = x および x = 0 (y 軸) は 2 つの双曲線扇形を形成します。 さらに、線 2y = x に沿って 2 つの放物線扇形があります。 第 1 象限の放物線扇形は不安定ですが、第 3 象限の放物線扇形は安定しています。

楕円形のタイプ:

線 y = 0 (x 軸) は、平面を 2 つの楕円セクターに分割します。 解の軌跡は原点から始まり、x 軸に接する円弧を形成し、原点に向かって収束します。

トリプルクロッシングタイプ:

楕円セクターは前のケースと同じですが、解曲線には劇的な変化が発生します。 解曲線は 3 つの尾根と谷を組み合わせて形成され、3 つの峰を持つ山の風景に似ています。 3 本の線が平面を 6 つの双曲セクターに分割します。

ノード タイプ 2:

線 y = 0 (x 軸) と x = 0 (y 軸) は 2 つの双曲線扇形を形成します。 また、線 y = -x に沿って 4 つの放物線扇形があり、解曲線がこの線に挟まれて原点に近づきます。 第 2 象限の放物線扇形は不安定ですが、第 4 象限の放物線扇形は安定しています。

パラボラタイプ:

線 y = -x は、平面を 2 つの放物線状のセクターに分割します。 線 y = 0 (x 軸) は、平面を 2 つの楕円セクターに分割します。 解の軌跡はこの線を越えて逆方向になります。

高次同時自律システム:

高次の自律形式以降は係数の入力はできません。 状況はご理解いただけると思います。 最終的には、ユーザーが自由に方程式を定義して解曲線をプロットできるページを Web 上に作成したいと考えていますが、今のところは遠い計画のようです。 とりあえず森本さんの本に出てくる方程式を紹介したいと思います。

定期的な解決策を備えた均質な自律型:

平面は、線 y = +/- x と x 軸および y 軸によって 8 つの放物線扇形に分割されます。

8 つの放物線セクターを備えた均質な自律形式:

平面は、直線 y = +/- x と x 軸および y 軸によって、それぞれ 2 つの双曲線セクターと 4 つの放物線セクターを含む 8 つのセクターに分割されます。




複合セクターを使用した自律型フォーム:

平面は、線 2y = (3 +/- sqrt{5})x と x 軸および y 軸によって 8 つのセクターに分割されます。 これは、2 つの双曲セクター、2 つの楕円セクター、および 4 つの放物線セクターで構成されます。

ヴィノグラードの自治形態:

これは前の複合セクター形式に似ていますが、dx/dt の右側に y^5 が追加されています。 ただし、y = 0 (x 軸) を分割線として、エドヴァルド ムンクの「叫び」を彷彿とさせる対称的な楕円扇形パターンが表示されます。